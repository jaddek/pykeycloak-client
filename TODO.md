# TODO

## Remaining High-Priority Work

1. Add full functional end-to-end test suite.
   - Target directory: `tests/functional`
   - Current state: only `tests/integration` smoke tests exist.
   - Goal: scenario-level flows (auth, refresh, user lifecycle, UMA, negative cases).
