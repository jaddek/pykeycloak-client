# API Reference

For the current callable surface, you can introspect methods at runtime:

1. Start MCP server (`uv run python mcp_server.py`)
2. Register a client key
3. Call `keycloak_list_methods`

This reflects the exact service methods available in your installed version.
